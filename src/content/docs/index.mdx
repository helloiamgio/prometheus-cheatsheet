---
title: Prometheus Cheatsheet
description: Query PromQL per Kubernetes / OpenShift, organizzate per oggetto.
hero:
  tagline: Query PromQL per Kubernetes / OpenShift — organizzate per oggetto (Pod, Deploy, Node, Job, Network, Storage, Istio, JVM), pronte per troubleshooting WR, capacity planning e alerting.
  actions:
    - text: Inizia da Pod / Container
      link: /pod/
      icon: right-arrow
      variant: primary
    - text: GitHub
      link: https://github.com/helloiamgio/prometheus-cheatsheet
      icon: external
---

import { Card, CardGrid } from '@astrojs/starlight/components';

## Filtro standard namespace applicativi

```promql
namespace!~"kube-.*|openshift-.*"
```

## Ordine di indagine in incidente (WR)

> CPU → Memoria → Restart → Rete → Storage → Job
> Correlare **spike + eventi** per identificare la root cause.

## Sezioni

<CardGrid>
  <Card title="Pod / Container" icon="seti:powershell">
    CPU/mem per pod e container, breakdown **multi-container** (app + sidecar Istio), restart/OOM.
  </Card>
  <Card title="Deploy / Workload" icon="rocket">
    Deployment, ReplicaSet, StatefulSet, DaemonSet, HPA, rollout e availability.
  </Card>
  <Card title="Node" icon="setting">
    CPU/RAM/capacity per nodo worker, runbook Node NotReady, pressure e disco, **etcd & Control Plane** (fonte: runbook ufficiali OpenShift).
  </Card>
  <Card title="Job / CronJob" icon="list-format">
    Job completati/falliti, CronJob sospesi o in ritardo, pod short-lived.
  </Card>
  <Card title="Network" icon="random">
    Traffico pod/interfacce (Multus-aware), DNS/CoreDNS, API server, **OVN-Kubernetes internals**, **Router/Ingress HAProxy**.
  </Card>
  <Card title="Storage" icon="document">
    Filesystem container, PVC capacity/usage, stato PersistentVolume.
  </Card>
  <Card title="Namespace & Cluster Capacity" icon="pencil">
    Consumi per namespace e capacity aggregata sui nodi worker.
  </Card>
  <Card title="Istio / Service Mesh" icon="puzzle">
    Overhead del sidecar Envoy in **core/GiB per namespace**, traffico, mTLS, control plane.
  </Card>
  <Card title="JVM / Java" icon="seti:java">
    Pause GC e **suspension %**, heap vs limit del container, leak o undersizing, metriche Micrometer/jmx_exporter.
  </Card>
  <Card title="Alert Rules" icon="warning">
    Regole di alerting pronte all'uso, per oggetto.
  </Card>
</CardGrid>

## Metriche cAdvisor — attenzione alle label

| Metrica | Cosa misura |
|---|---|
| `container_cpu_usage_seconds_total` | CPU cumulativa (counter) → serve `rate()`/`irate()` |
| `container_memory_working_set_bytes` | memoria "attiva" → quella confrontata col limit per OOM |
| `container_memory_rss` | memoria residente reale del processo |
| `container_spec_cpu_quota` / `container_spec_cpu_period` | limit CPU effettivo (quota/period = core) |
| `container_spec_memory_limit_bytes` | limit memoria del container |
| `container_cpu_cfs_throttled_seconds_total` | tempo throttled dal CFS scheduler (evento certo, non stimato) |

Filtri comuni da tenere sempre a mente:
- `container!="", container!="POD"` → esclude il container `pause` e le serie senza container (livello pod)
- in mesh Istio: `container="istio-proxy"` isola il sidecar Envoy, il resto è il/i container applicativi
- per workload Java: cAdvisor **non vede l'heap** — le metriche `container_*` misurano il cgroup, non la JVM

Fonti: [openshift-cheatsheet.pages.dev/prometheus-utils](https://openshift-cheatsheet.pages.dev/prometheus-utils/) · [github.com/helloiamgio/prometheus-cheatsheet](https://github.com/helloiamgio/prometheus-cheatsheet) · [github.com/openshift/runbooks](https://github.com/openshift/runbooks) · [Red Hat Docs — OpenShift Monitoring](https://docs.redhat.com/en/documentation/openshift_container_platform/latest/html/monitoring/index) · [Red Hat OpenShift Service Mesh — Performance & Scalability](https://docs.openshift.com/rosa/service_mesh/v2x/ossm-performance-scalability.html)

> Alcune soluzioni KCS di Red Hat (`access.redhat.com/solutions/...`) sono citate per titolo/numero dove pertinenti, ma il contenuto completo richiede una subscription attiva — non riproducibile qui.
